# Google Cloud OAuth2 access token (cloud-platform / devstorage scope). Obtained
# via ADC, gcloud auth print-access-token, or a service account. Referenced as a
# credproxy placeholder and swapped for the real token at TLS egress.
GOOGLE_CLOUD_ACCESS_TOKEN
