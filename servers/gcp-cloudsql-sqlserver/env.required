# Google Cloud OAuth 2.0 access token with IAM permissions for Cloud SQL.
# Referenced from the Authorization header; credproxy swaps the placeholder for
# the real token at TLS egress, so it never enters the agent sandbox.
GOOGLE_CLOUD_ACCESS_TOKEN
