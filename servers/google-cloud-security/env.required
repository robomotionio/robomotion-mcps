# Chronicle SecOps project ID, customer/instance UUID, and region. Injected
# host-side by the credstdio bridge; never enter the agent sandbox.
CHRONICLE_PROJECT_ID
CHRONICLE_CUSTOMER_ID
CHRONICLE_REGION
