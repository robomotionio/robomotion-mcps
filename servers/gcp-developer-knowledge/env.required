# Google Cloud API key restricted to the Developer Knowledge API. Referenced
# from the X-Goog-Api-Key header; credproxy swaps the placeholder for the real
# key at TLS egress, so it never enters the agent sandbox.
GOOGLE_API_KEY
