# Google Cloud access token, referenced from the Authorization header. credproxy
# swaps the placeholder for the real token at TLS egress; never enters the sandbox.
GOOGLE_CLOUD_ACCESS_TOKEN
# Google Cloud project ID, sent as the x-goog-user-project quota/billing header.
GOOGLE_CLOUD_PROJECT
