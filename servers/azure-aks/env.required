# Azure service principal credentials used to authenticate to the Azure Resource
# Manager. Injected host-side by the credstdio bridge; never enter the agent sandbox.
AZURE_CLIENT_ID
AZURE_CLIENT_SECRET
AZURE_TENANT_ID
