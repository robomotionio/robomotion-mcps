# Google Cloud OAuth 2.0 access token scoped to the Chronicle API. Referenced as
# an Authorization: Bearer header; credproxy swaps the placeholder for the real
# token at TLS egress, so it never enters the agent sandbox.
GOOGLE_CLOUD_ACCESS_TOKEN
# Regional endpoint of your SecOps tenant (e.g. us, europe). Binds the URL host.
CHRONICLE_REGION
