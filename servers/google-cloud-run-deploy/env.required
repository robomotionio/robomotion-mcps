# Google Cloud project ID, region, and the Application Default Credentials
# service-account key path. Injected host-side by the credstdio bridge; never
# enter the agent sandbox.
GOOGLE_CLOUD_PROJECT
GOOGLE_CLOUD_REGION
GOOGLE_APPLICATION_CREDENTIALS
