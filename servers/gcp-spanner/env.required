# OAuth 2.0 access token for a Google Cloud identity with Spanner scopes.
# Referenced as Authorization=Bearer; credproxy swaps the placeholder for the
# real token at TLS egress, so it never enters the agent sandbox.
GOOGLE_CLOUD_ACCESS_TOKEN
# Google Cloud project ID, sent as the x-goog-user-project quota/billing header.
GOOGLE_CLOUD_PROJECT
