# Google Cloud OAuth 2.0 access token (gcloud ADC or service account). Referenced
# from the Authorization header; credproxy swaps the placeholder for the real
# token at TLS egress, so it never enters the agent sandbox.
GCP_ACCESS_TOKEN
