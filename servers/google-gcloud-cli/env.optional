# Optional path to a Google Cloud service-account key file, used when
# authenticating as a service account instead of the host gcloud user / ADC.
# Injected host-side by the credstdio bridge; never enters the agent sandbox.
GOOGLE_APPLICATION_CREDENTIALS
