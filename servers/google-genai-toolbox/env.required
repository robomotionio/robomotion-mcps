# PostgreSQL connection host, database, user, and password for the --prebuilt
# postgres tools. Injected host-side by the credstdio bridge; never enter the
# agent sandbox.
POSTGRES_HOST
POSTGRES_DATABASE
POSTGRES_USER
POSTGRES_PASSWORD
