# Google Cloud OAuth 2.0 access token (scope bigtable.admin), e.g. from
# `gcloud auth application-default print-access-token` or a service account.
# credproxy swaps it into the Authorization header at egress; never enters the sandbox.
GOOGLE_CLOUD_ACCESS_TOKEN
