# Google Cloud OAuth 2.0 access token used as the Authorization bearer. Referenced
# as a credproxy placeholder and swapped for the real token at TLS egress; never
# enters the agent sandbox.
GOOGLE_CLOUD_ACCESS_TOKEN
# Google Cloud project ID, sent as the x-goog-user-project quota/billing project.
GOOGLE_CLOUD_PROJECT
