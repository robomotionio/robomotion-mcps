# Entra (Azure AD) service-principal credentials used by the Azure Identity SDK.
# Injected host-side by the credstdio bridge; never enter the agent sandbox.
AZURE_TENANT_ID
AZURE_CLIENT_ID
AZURE_CLIENT_SECRET
