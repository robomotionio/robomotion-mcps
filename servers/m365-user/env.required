# Microsoft Entra tenant ID (GUID) bound into the Agent365 endpoint path.
# Resolved from a bound vault item; enters the sandbox only as a credproxy
# placeholder, swapped for the real value at TLS egress.
MICROSOFT_TENANT_ID
