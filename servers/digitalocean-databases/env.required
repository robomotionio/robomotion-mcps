# DigitalOcean personal access token. Referenced from the Authorization header;
# credproxy swaps the placeholder for the real token at TLS egress, so it never
# enters the agent sandbox.
DIGITALOCEAN_API_TOKEN
