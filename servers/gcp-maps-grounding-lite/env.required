# Google Maps Platform API key for a Cloud project with the Maps Grounding Lite
# API enabled. Referenced as the X-Goog-Api-Key header; credproxy swaps the
# placeholder for the real key at TLS egress. Never enters the agent sandbox.
GOOGLE_MAPS_API_KEY
